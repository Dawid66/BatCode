package Warmup1;

public class start {
    public boolean mixStart(String str) {
        if(str.substring(1,2).equals("ix"))return true;
        else if(str.length()<3)return false;

     else return false;
    }
}
